{
    // === Local Development Container Configuration ===
    // This file allows you to customize your development environment without modifying the base configuration.
    // It will be automatically merged with the main devcontainer.json
    // Add this file to .gitignore to keep your personal settings private.

    // === Build Configuration ===
    // Uncomment to use your local Dockerfile with custom installations
    // "build": {
    //     "dockerfile": "Dockerfile.local"
    // },

    // === VS Code Customizations ===
    "customizations": {
        "vscode": {
            // Add your personal VS Code extensions
            // These will be installed in addition to the ones in devcontainer.json
            "extensions": [
                // Development Tools
                // "eamodio.gitlens",
                // "ms-python.python",
                
                // Documentation & Productivity
                // "streetsidesoftware.code-spell-checker",
                // "davidanson.vscode-markdownlint",

                // Theme & Visual Customization
                // "vscode-icons-team.vscode-icons",
                // "pkief.material-icon-theme"
            ],

            // Override or add VS Code settings
            "settings": {
                // Editor preferences
                // "editor.fontSize": 14,
                // "editor.lineHeight": 1.6,
                // "editor.wordWrap": "on",
                
                // Terminal settings
                // "terminal.integrated.fontSize": 14,
                // "terminal.integrated.lineHeight": 1.2,
                
                // Language-specific settings
                // "python.formatting.provider": "black",
                // "javascript.updateImportsOnFileMove.enabled": "always"
            }
        }
    },

    // === Environment Variables ===
    // Add your local environment variables
    // These will be merged with or override the ones in devcontainer.json
    // "remoteEnv": {
    //     "NODE_ENV": "development",
    //     "PYTHON_ENV": "development",
    //     "AZURE_SUBSCRIPTION": "my-subscription",
    //     "DEBUG": "true"
    // },

    // === Mount Points ===
    // Add local folders you want to mount in the container
    // These will be merged with the mounts in devcontainer.json
    // "mounts": [
    //     "source=${localEnv:HOME}/projects,target=/workspace/projects,type=bind,consistency=cached",
    //     "source=${localEnv:HOME}/data,target=/workspace/data,type=bind,consistency=cached"
    // ],

    // === Container Features ===
    // Override or add container features
    // "features": {
    //     "ghcr.io/devcontainers/features/python:1": {
    //         "version": "3.11",
    //         "installJupyterlab": true
    //     },
    //     "ghcr.io/devcontainers/features/node:1": {
    //         "version": "20"
    //     }
    // },

    // === Post-Create Commands ===
    // Run additional commands after container is created
    // "postCreateCommand": "npm install && pip install -r requirements-dev.txt",

    // === Port Forwarding ===
    // Forward additional ports from the container
    // "forwardPorts": [3000, 8000],
    
    // === User Settings ===
    // Override default user settings
    // "remoteUser": "vscode"
}